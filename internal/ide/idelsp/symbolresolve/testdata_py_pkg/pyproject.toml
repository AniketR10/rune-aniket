[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mypkg"
version = "0.1.0"
description = "Fixture package for dotted module path symbol resolution"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]