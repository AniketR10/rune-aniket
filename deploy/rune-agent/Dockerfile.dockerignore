.git
target
bin
# Stale CMake cache and build outputs from a host-side make invocation must
# not pollute the cross-compile inside the Linux builder image. The Dockerfile
# rebuilds these from scratch.
cmd/rune-agent/llm/llamacpp/llama.cpp/_build
cmd/rune-agent/llm/llamacpp/libs
