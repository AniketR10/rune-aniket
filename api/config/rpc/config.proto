syntax = "proto3";

option go_package = "unstable.build/go-tui/api/config/rpc";

package config;

message GetRequest {
}

message GetResponse {
	string data = 1;
}

service Config {
	rpc Get(GetRequest) returns (GetResponse);
}
