syntax = "proto3";

option go_package = "github.com/ernestrc/go-tui/plugin/rpc";

package plugin;

message GetRequest {
}

message GetResponse {
	string data = 1;
}

service Config {
	rpc Get(GetRequest) returns (GetResponse);
}
